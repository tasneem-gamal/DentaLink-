import 'package:dentalink/features/AI_scan/ui/widgets/ai_scan_view_body.dart';
import 'package:flutter/material.dart';

class AiScanView extends StatelessWidget {
  const AiScanView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: AiScanViewBody(),
    );
  }
}